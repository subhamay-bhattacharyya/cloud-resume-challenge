name: Terraform Apply
run-name: Terraform Apply - ${{ github.ref_name }}

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy to (e.g., AWS, GCP, Azure)"
        required: true
        type: environment


jobs:
  # apply:
  #   name: 🚧 Run Terraform Apply
  #   uses: subhamay-bhattacharyya-gha/tf-apply-wf/.github/workflows/terraform-apply.yaml@main
  #   with:
  #     environment: ${{ inputs.environment }}
  #     terraform-dir: ${{ inputs.terraform-dir }}
  #     tf-vars-file: ${{ inputs.tf-vars-file }}
  #     ci-pipeline: false
  #   secrets:
  #     aws-role-arn: ${{ secrets.AWS_ROLE_ARN }}
  #     infracost-api-key: ${{ secrets.INFRACOST_API_KEY }}
  #     infracost-gist-id: ${{ secrets.INFRACOST_GIST_ID }}
  setup-environments:
    steps:
      - name: Print environment
        run: |
          echo "Setting up environments for ${{ github.event.inputs.environment }}"
