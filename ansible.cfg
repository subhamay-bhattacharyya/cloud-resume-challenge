[defaults]
# Use the built-in callback (don’t use community.general.yaml)
stdout_callback = ansible.builtin.default

# Tell the default callback to render results as YAML
result_format = yaml

# Optional: cleaner output
bin_ansible_callbacks = True
display_skipped_hosts = False
